{
  "name": "koncept-creator",
  "version": "0.0.0",
  "description": "lib for creating concepts",
  "main": "index.js",
  "watch": {
    "build-dev": {
      "patterns": ["src"]
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "BABEL_ENV=production babel src --out-file lib/app.js",
    "build-dev": "babel src --out-file lib/app.js",
    "build-watch": "npm-watch build-dev"
  },
  "repository": {
    "type": "git",
    "url": "github.com:ntbm/koncept-creator.git"
  },
  "author": "Sascha Hahne <hahne.sascha@web.de>",
  "license": "NO LICENSE",
  "dependencies": {
    "vis": "^4.21.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-concat": "^1.0.3",
    "babel-minify": "^0.2.0",
    "babel-preset-env": "^1.6.1",
    "npm-watch": "^0.3.0"
  }
}
